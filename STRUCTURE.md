# 📁 СТРУКТУРА ПРОЕКТА

```
bot677/
├── bot.py                    # 🚀 Главный файл - запуск бота
├── config.py                 # ⚙️ Конфигурация (токен, цены, сообщения)
├── states.py                 # 📊 FSM состояния для заказа
├── keyboards.py              # 🎮 Клавиатуры (кнопки, меню)
├── handlers.py               # 📨 Обработчики основных команд
├── order_handlers.py         # 📦 Обработчики процесса заказа
├── order_service.py          # 🧮 Бизнес-логика заказов (расчеты, форматирование)
├── utils.py                  # 🔧 Утилиты и вспомогательные функции
├── debug_tools.py            # 🐛 Инструменты отладки и тестирования
│
├── requirements.txt          # 📦 Зависимости Python
├── .env.example              # 📝 Пример конфига для переменных окружения
├── run.bat                   # 🏃 Скрипт запуска на Windows
├── run.sh                    # 🏃 Скрипт запуска на Linux/Mac (если есть)
│
├── README.md                 # 📖 Полная документация
├── QUICKSTART.md             # ⚡ Быстрый старт в 3 шага
└── STRUCTURE.md              # 📁 Этот файл
```

## 🔍 ОПИСАНИЕ ФАЙЛОВ

### 🚀 bot.py
**Главный файл запуска бота**

- Инициализирует бота и диспетчер
- Подключает все обработчики (handlers и order_handlers)
- Запускает polling для получения обновлений
- Устанавливает команды бота в меню

```bash
python bot.py  # Запуск бота
```

---

### ⚙️ config.py
**Конфигурация и константы**

Содержит:
- `BOT_TOKEN` - токен вашего бота
- `ADMIN_CHAT_ID` - ID админ-группы (⚠️ ВАЖНО!)
- `BASE_PRICES` - базовые цены на услуги
- `URGENCY_MULTIPLIERS` - коэффициенты за срочность
- `PAYMENT_DETAILS` - реквизиты оплаты
- `MESSAGES` - все текстовые сообщения для пользователей

**Именно здесь вы кастомизируете бота!**

---

### 📊 states.py
**FSM (Finite State Machine) состояния**

Определяет возможные состояния диалога:
- `OrderStates.waiting_for_work_type` - выбор типа работы
- `OrderStates.waiting_for_urgency` - выбор срочности
- `OrderStates.waiting_for_details` - ввод деталей заказа
- `OrderStates.waiting_for_confirmation` - подтверждение
- `OrderStates.waiting_for_payment` - ввод платежа
- `SupportStates.waiting_for_message` - ожидание сообщения поддержки

**Это скелет диалога.**

---

### 🎮 keyboards.py
**Клавиатуры и кнопки**

Функции для создания клавиатур:
- `get_main_menu_keyboard()` - главное меню (Заказать, Поддержка, Оплата)
- `get_work_type_keyboard()` - выбор типа работы
- `get_urgency_keyboard()` - выбор срочности
- `get_confirmation_keyboard()` - подтверждение заказа
- `get_payment_keyboard()` - кнопка "Я оплатил"
- `get_back_to_menu_keyboard()` - возврат в меню

**Здесь изменяется UI.**

---

### 📨 handlers.py
**Обработчики основных команд**

Функции для обработки:
- `/start` - приветствие и главное меню
- "📦 Заказать" - начало оформления заказа
- "❓ Поддержка" - переход в режим поддержки
- "💳 Способы оплаты" - показ информации об оплате

**Точка входа для пользователя.**

---

### 📦 order_handlers.py
**Обработчики процесса заказа (FSM)**

Обрабатывает весь процесс оформления:
1. Выбор типа работы (`select_work_type`)
2. Выбор срочности (`select_urgency`)
3. Ввод деталей (`receive_details`)
4. Подтверждение заказа (`confirm_order`)
5. Подтверждение оплаты и отправка в админ-чат (`payment_confirmed`)

**Основная логика бота.**

---

### 🧮 order_service.py
**Бизнес-логика заказов**

Класс `OrderService` с методами:
- `calculate_price()` - расчет цены с учетом срочности
- `create_order()` - создание заказа
- `get_order()` - получение заказа по ID
- `format_order_for_admin()` - форматирование для админ-чата
- `format_order_summary()` - форматирование для клиента

**Здесь живут все расчеты.**

---

### 🔧 utils.py
**Утилиты**

Вспомогательные функции:
- `get_admin_chat_id_from_message()` - инструкция для получения ID
- `format_order_details_for_display()` - красивое отображение заказа
- `log_order()` - логирование заказов
- `validate_price()` - проверка цены
- Различные константы с эмодзи

**Переиспользуемый код.**

---

### 🐛 debug_tools.py
**Инструменты для отладки**

Интерактивное меню с функциями:
1. Получение ADMIN_CHAT_ID (самое важное!)
2. Проверка токена бота
3. Отправка тестовых сообщений
4. Чтение лога заказов
5. Статистика заказов

Запуск:
```bash
python debug_tools.py
```

**Для диагностики и тестирования.**

---

### 📦 requirements.txt
**Зависимости проекта**

```
aiogram==3.3.0
aiohttp==3.9.1
python-dotenv==1.0.0
```

Установка:
```bash
pip install -r requirements.txt
```

---

### 📖 README.md
**Полная документация**

- Описание всех возможностей
- Инструкция по установке
- Как настроить конфиг
- Примеры использования
- Решение проблем

**Читайте перед использованием.**

---

### ⚡ QUICKSTART.md
**Быстрый старт**

Точный пошаговый гайд:
1. Установка зависимостей
2. Получение ADMIN_CHAT_ID (ШАГ 1-2 минуты!)
3. Запуск бота
4. Проверка работоспособности

**Начните отсюда!**

---

## 🔄 ПОТОК ДАННЫХ

```
Пользователь (Telegram)
    ↓
    ↓ /start команда
    ↓
bot.py (диспетчер)
    ↓
    ├→ handlers.py (основные команды)
    │   └→ start_command, order_start, support_start и т.д.
    │
    └→ order_handlers.py (обработка заказа)
        ├→ select_work_type (FSM состояние)
        ├→ select_urgency
        ├→ receive_details
        ├→ confirm_order
        └→ payment_confirmed
            ↓
            └→ order_service.py (создание заказа, форматирование)
                └→ OrderService.create_order()
                    ↓
                    └→ Bot.send_message() → ADMIN_CHAT_ID (админ-группа)
```

---

## 🎯 РАБОЧИЙ ПРОЦЕСС

### Для разработчика

1. **Изменяю цены** → `config.py` → `BASE_PRICES`
2. **Изменяю текст сообщений** → `config.py` → `MESSAGES`
3. **Добавляю новую кнопку** → `keyboards.py` → `order_handlers.py`
4. **Добавляю новое поле в заказ** → `states.py`, `order_handlers.py`, `order_service.py`
5. **Тестирую** → `debug_tools.py`

### Для администратора

1. **Получаю уведомление** о заказе в админ-группе
2. **Выполняю работу**
3. **Отправляю результат** клиенту (вручную, пока)

---

## 💾 СОХРАНЕНИЕ ДАННЫХ

**В текущей версии (MVP):**
- Заказы хранятся в памяти (переменная `orders_storage` в `order_service.py`)
- При перезагрузке бота заказы теряются ❌

**Для продакшена добавьте:**
- SQLite базу (простой вариант)
- PostgreSQL или MongoDB (для масштабирования)
- Логирование в JSON файлы (для резервной копии)

---

## 🚀 ЗАПУСК

### На Windows
```bash
python bot.py
# или
run.bat
```

### На Linux/Mac
```bash
python3 bot.py
```

---

## 📊 ПРИМЕРЫ КОМАНД

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python bot.py

# Отладка
python debug_tools.py

# Проверка синтаксиса
python -m py_compile bot.py
```

---

## ✅ ЧЕКЛИСТ ПЕРВОГО ЗАПУСКА

- [ ] `pip install -r requirements.txt` - установка зависимостей
- [ ] Получение `ADMIN_CHAT_ID` (см. QUICKSTART.md)
- [ ] Заполнение `ADMIN_CHAT_ID` в `config.py`
- [ ] Запуск: `python bot.py`
- [ ] Тест: `/start` в Telegram
- [ ] Тестовый заказ → проверка админ-группы
- [ ] Кастомизация `config.py` под ваши нужды

---

## 🎓 ОБУЧЕНИЕ

Если вы хотите понять, как это работает:

1. Начните с `bot.py` - точка входа
2. Затем `handlers.py` - обработка команд
3. Потом `order_handlers.py` - основная логика
4. Финально `order_service.py` - бизнес-логика

Каждый файл имеет комментарии и docstrings.

---

**Готово к работе! 🚀**
