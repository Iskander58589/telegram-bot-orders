# ⚡ ЧИТАЙ СНАЧАЛА - РАЗВЕРТЫВАНИЕ В 3 ШАГА

## Вся информация, которая тебе нужна - вот:

### **3 ШАГА К ГОТОВОМУ БОТУ** ⏱️ 20 минут

---

## 📍 ШАГ 1: GitHub (10 минут)

### 1.1. Создать репо
- github.com → New → Название: `telegram-bot-orders`
- **ОБЯЗАТЕЛЬНО: "Private"** ✓
- Create

### 1.2. Залить код
```powershell
cd c:\Users\ASUS\Videos\.gallery\4e2e315c-1878-42fb-8de7-c57a9b83f6dc\bot677

git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot-orders.git
git branch -M main
git push -u origin main
```

### 1.3. Добавить Secrets
- GitHub репо → Settings → Secrets and variables → Actions
- "New repository secret"
- Добавить:
  ```
  BOT_TOKEN = 8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM
  ADMIN_CHAT_ID = -1003898978688
  ```

✅ **GitHub готов!**

---

## 🖥️ ШАГ 2: Hetzner Сервер (5 минут)

### 2.1. Зарегистрироваться
- hetzner.com → Sign Up
- Подтвердить почту

### 2.2. Создать SSH ключ
```powershell
ssh-keygen -t rsa -b 4096 -f $env:USERPROFILE\.ssh\id_rsa
type $env:USERPROFILE\.ssh\id_rsa.pub | clip
# Ключ скопирован в буфер обмена
```

### 2.3. Создать сервер
- Hetzner консоль → Create Server
- Ubuntu 22.04 LTS
- Frankfurt am Main (локация)
- CX11 (размер)
- Добавить SSH Key (вставить скопированный выше)
- Create & Buy

✅ **Сервер готов! Сохрани IP адрес!**

---

## 🚀 ШАГ 3: Развертывание (5 минут)

### 3.1. Подключиться к серверу
```powershell
ssh root@ТВ​ОЙ_IP_АДРЕС
# Пример: ssh root@138.201.123.456
```

### 3.2. Развернуть код (ВЫБЕРИ ОДИН)

**ВАРИАНТ A - Одна команда (самый быстрый):**
```bash
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/telegram-bot-orders/main/setup.sh && chmod +x setup.sh && BOT_TOKEN="8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM" ADMIN_CHAT_ID="-1003898978688" bash setup.sh
```

Скрипт сам:
- Установит Python ✓
- Клонирует код ✓
- Создаст окружение ✓
- Запустит бота ✓

**ИЛИ**

**ВАРИАНТ B - Вручную по шагам** (см. файл `DEPLOY_SUMMARY.md`)

---

## ✅ ПРОВЕРКА

```bash
# На сервере
systemctl status telegram-bot

# Должно быть: "active (running)"
```

Открываешь Telegram → пишешь боту `/start` → **ГОТОВО!** 🎉

---

## 📚 Полная информация в файлах:

- **`DEPLOY_SUMMARY.md`** ← Полный гайд (все шаги с деталями)
- **`HETZNER_DEPLOYMENT.md`** ← Расширенный гайд
- **`QUICK_SETUP.md`** ← Чеклист
- **`HOW_IT_WORKS.md`** ← Как всё работает

---

## 🔐 Безопасность (ВАЖНО!)

✅ Мы уже сделали:
- Токен в переменных окружения (не в коде)
- GitHub Secrets для хранения токенов
- `.env` защищен (не попадает на GitHub)
- `.gitignore` настроен правильно

❌ Что НЕ делать:
- Не публикуй токены в коде
- Не заливай `.env` на GitHub
- Не делай репо public (если там токены)
- Не делись SSH ключами

---

## 🆘 Проблемы?

### Бот не работает?
```bash
journalctl -u telegram-bot -n 50
```

### Токен неправильный?
```bash
cat /home/telegram-bot/.env
```

### Git проблемы?
```bash
cd /home/telegram-bot && git status
```

---

**Готово! Остальное в файлах 📄**

Есть конкретный вопрос? Спрашивай! 🚀
