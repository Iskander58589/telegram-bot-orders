# ⚡ ШПАРГАЛКА - САМОЕ ВАЖНОЕ НА ОДНОЙ СТРАНИЦЕ

## 🎯 ТРИ ШАГА К БОТУ 24/7

```
ШАГ 1: GitHub (10 мин)
├─ Создать репо (Private!)
├─ git push весь код
└─ Добавить Secrets (BOT_TOKEN, ADMIN_CHAT_ID)

ШАГ 2: Hetzner (5 мин)
├─ Зарегистрироваться
├─ Создать SSH ключ
└─ Создать сервер (CX11)

ШАГ 3: Развертывание (5 мин)
├─ ssh root@IP
└─ bash setup.sh
    ↓
   ✅ БОТ РАБОТАЕТ!
```

---

## 🔑 КРИТИЧЕСКИЕ КОМАНДЫ

### На локальном ПК (PowerShell):

```powershell
# Инициализация Git
git init
git add .
git commit -m "Initial"
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot-orders.git
git branch -M main
git push -u origin main

# SSH ключ
ssh-keygen -t rsa -b 4096 -f $env:USERPROFILE\.ssh\id_rsa
type $env:USERPROFILE\.ssh\id_rsa.pub | clip
```

### На сервере (Bash):

```bash
# Подключение
ssh root@IP_АДРЕС

# Развертывание (автоматическое)
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/telegram-bot-orders/main/setup.sh
BOT_TOKEN="твой_токен" ADMIN_CHAT_ID="твой_id" bash setup.sh

# Проверка
systemctl status telegram-bot

# Логи
journalctl -u telegram-bot -f

# Обновление кода
cd /home/telegram-bot && git pull origin main && systemctl restart telegram-bot
```

---

## 📋 КРИТИЧЕСКИЕ ФАЙЛЫ

| Файл | Что делает | На GitHub? |
|------|-----------|-----------|
| `.env` | Хранит токены | ❌ НЕТ (в .gitignore) |
| `.env.example` | Пример .env | ✅ ДА |
| `config.py` | Читает из .env | ✅ ДА |
| `setup.sh` | Развертывает | ✅ ДА |
| `.gitignore` | Защита от утечек | ✅ ДА |

---

## 🔐 БЕЗОПАСНОСТЬ

```
❌ НЕПРАВИЛЬНО:
BOT_TOKEN = "8283115795:AAH..." в коде

✅ ПРАВИЛЬНО:
BOT_TOKEN = os.getenv("BOT_TOKEN")
```

**Правило #1:** Токен НЕ должен быть в коде
**Правило #2:** Репо должен быть PRIVATE
**Правило #3:** Используй GitHub Secrets
**Правило #4:** .env в .gitignore

---

## 🚀 СОКРАТO

```
1. git push код на GitHub (Private!)
2. Добавить 2 Secrets в GitHub
3. Заказать CX11 на Hetzner (€3.50/месяц)
4. ssh и bash setup.sh
5. Тестировать в Telegram
```

**ГОТОВО!** ✅

---

## 💾 ФАЙЛЫ КОНФИГУРАЦИИ

### .env.example (в репо, ПУБЛИЧНЫЙ):
```
BOT_TOKEN=ЗАМЕНИ_НА_СВОЙ
ADMIN_CHAT_ID=ЗАМЕНИ_НА_СВОЙ
```

### .env (на сервере, ПРИВАТНЫЙ):
```
BOT_TOKEN=8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM
ADMIN_CHAT_ID=-1003898978688
```

---

## 🆘 БЫСТРАЯ ПОМОЩЬ

| Проблема | Решение |
|----------|---------|
| Бот не запускается | `journalctl -u telegram-bot -n 50` |
| Ошибка токена | `cat /home/telegram-bot/.env` |
| Нужно обновить код | `cd /home/telegram-bot && git pull origin main` |
| Перезапустить бота | `systemctl restart telegram-bot` |
| Проверить что работает | `systemctl status telegram-bot` |

---

## 📊 СТРУКТУРА

```
Локально:             GitHub:               На Hetzner:
bot.py ─────push──→ Исходный код ───pull──→ /home/telegram-bot/bot.py
config.py            (репо)                 Виртуальное окружение
requirements.txt                            Systemd сервис
.gitignore                                  .env (с токенами)

Secrets:
BOT_TOKEN ────────────────────────────────→ переменные окружения
ADMIN_CHAT_ID ─────────────────────────────→ на сервере
```

---

## ⏱️ ВРЕМЯ

- GitHub: 10 минут
- Hetzner: 5 минут
- Развертывание: 5 минут
- **ВСЕГО: 20 минут**

---

## 💰 СТОИМОСТЬ

- GitHub: БЕСПЛАТНО ✓
- Hetzner CX11: €3.50/месяц ✓
- **ВСЕГО: €3.50/месяц**

---

## 🎯 ПРОВЕРКА

```bash
# На сервере
systemctl status telegram-bot

# Должно быть:
# ● telegram-bot.service - Telegram Orders Bot
#    Loaded: loaded (/etc/systemd/system/telegram-bot.service; enabled; vendor preset: enabled)
#    Active: active (running) since ...  ← ВАЖНО!

# Тест в Telegram:
# Пиши боту /start → если ответит = ВСЕ РАБОТАЕТ!
```

---

## 📚 ДОКУМЕНТАЦИЯ

- `START_HERE_DEPLOY.md` ← НАЧНИ ОТСЮДА
- `DEPLOY_SUMMARY.md` ← Полный гайд
- `FAQ_DEPLOYMENT.md` ← Вопросы и ответы
- `HOW_IT_WORKS.md` ← Как все работает

---

## ✨ ВАЖНЫЕ МОМЕНТЫ

1. **Приватный репо** - только ты видишь, даже если токены будут
2. **Secrets в GitHub** - не попадают в код
3. **setup.sh** - автоматизирует все
4. **systemd** - запускает бота 24/7 и перезапускает при сбое
5. **git pull** - обновляет код на сервере

---

## 🚀 ГОТОВО!

Все готово к развертыванию.
Начни с `START_HERE_DEPLOY.md`.

**УДАЧИ!** 🎉
