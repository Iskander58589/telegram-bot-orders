# 🚀 БЫСТРЫЙ СТАРТ - 3 шага до запуска бота

## ШАГ 1: Установка зависимостей (1 минута)

Откройте PowerShell (Windows) или терминал (Linux/Mac) в папке `bot677`:

```bash
pip install -r requirements.txt
```

Ждите завершения установки (должна установиться библиотека `aiogram==3.3.0` и её зависимости).

---

## ШАГ 2: Получение ADMIN_CHAT_ID (3 минуты)

**⚠️ ЭТО САМОЕ ВАЖНОЕ!** Без этого заказы не будут приходить админам.

### Способ 1: Через curl (рекомендуется)

1. Создайте в Telegram **закрытую группу** (например, "Мои заказы")
2. Добавьте туда бота (он должен быть администратором)
3. Отправьте в группу любое сообщение
4. Откройте PowerShell и выполните:

```bash
curl "https://api.telegram.org/bot8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM/getUpdates"
```

5. В ответе найдите строку типа:
```
"chat":{"id":-1001234567890
```

6. Число после `"id":` (например `-1001234567890`) и есть ADMIN_CHAT_ID

### Способ 2: Через специальный бот

1. Напишите боту @userinfobot в Telegram
2. Добавьте этого бота в вашу группу
3. Он покажет ID группы в сообщении

---

## ШАГ 3: Установка ADMIN_CHAT_ID в конфиг (1 минута)

Откройте файл `config.py` в редакторе VS Code:

```python
# Найдите эту строку (примерно на строке 10):
ADMIN_CHAT_ID = -1001234567890  # Замените на ID вашего админ-чата

# Замените число -1001234567890 на ваше число из шага 2
# Например:
ADMIN_CHAT_ID = -1001987654321
```

Сохраните файл (Ctrl+S).

---

## ШАГ 4: ЗАПУСК БОТА! 🎉

В PowerShell/терминале выполните:

```bash
python bot.py
```

Или на Linux/Mac:

```bash
python3 bot.py
```

**Вы должны увидеть:**
```
2026-01-31 15:53:00 - aiogram.dispatcher - INFO - 🚀 Бот запущен!
```

Если всё заработало - **ура!** Бот готов к работе! 🚀

---

## ПРОВЕРКА: Бот действительно работает?

1. Откройте Telegram
2. Найдите вашего бота по username
3. Нажмите `/start`
4. Вы должны увидеть:
   - Приветственное сообщение
   - Три кнопки: "Заказать", "Поддержка", "Способы оплаты"

Если кнопки появились - **бот работает!** ✅

---

## ПРОВЕРКА: Админ-чат настроен правильно?

1. В Telegram откройте вашу админ-группу
2. Нажмите на бота: `/start`
3. Нажмите кнопку "Заказать"
4. Заполните:
   - Тип работы: выберите любой
   - Срочность: выберите любую
   - Детали: напишите "тест"
5. Нажмите "Подтвердить"
6. Нажмите "Я оплатил"

**Проверьте админ-группу:** должно появиться сообщение вида:

```
🔔 НОВЫЙ ЗАКАЗ

👤 Клиент: @your_username
🆔 User ID: `123456789`
...
```

Если сообщение появилось - **всё работает!** 🎉

---

## ❌ Если что-то не работает:

### Ошибка: "ModuleNotFoundError: No module named 'aiogram'"

**Решение:**
```bash
pip install -r requirements.txt
```

Убедитесь, что установка завершилась успешно.

### Ошибка: "Токен неверный"

**Решение:**
Проверьте в `config.py`:
```python
BOT_TOKEN = "8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM"
```

Токен должен быть ровно такой, как выше.

### Заказы не приходят в админ-группу

**Решение:**
1. Убедитесь, что `ADMIN_CHAT_ID` заполнен (не -1001234567890)
2. Проверьте, что бот добавлен в группу администратором
3. Скопируйте правильный ID по инструкции из ШАГ 2

### Бот не отвечает на сообщения

**Решение:**
1. Перезагрузите бота (Ctrl+C в терминале, потом заново `python bot.py`)
2. Проверьте интернет
3. Убедитесь, что нет опечаток в токене

---

## 📝 Что дальше?

Когда бот работает:

### Для администратора
1. Следите за заказами в админ-группе
2. Выполняйте работы
3. Отправляйте результаты клиентам (пока вручную)

### Для кастомизации
Отредактируйте в `config.py`:
- `BASE_PRICES` - измените цены
- `URGENCY_MULTIPLIERS` - измените коэффициенты срочности
- `PAYMENT_DETAILS` - вставьте ваши реквизиты оплаты
- `MESSAGES` - измените текст сообщений

Все изменения вступают в силу после перезагрузки бота.

---

## 📚 Полная документация

Подробные инструкции в файле `README.md`.

---

**Готово!** Если у вас не получается - просто следуйте шагам выше ещё раз, особенно обратите внимание на ADMIN_CHAT_ID! 🎯
