# 📦 Telegram-бот для приёма заказов на учебные работы

Полнофункциональный Telegram-бот на Python с использованием aiogram, который автоматически принимает заказы на учебные работы (презентации, доклады, эссе и т.д.), собирает информацию, рассчитывает цену и отправляет заказ администратору.

## 🚀 Возможности

- ✅ **Автоматический приём заказов** через диалоговый интерфейс
- ✅ **FSM (Finite State Machine)** для управления состояниями заказа
- ✅ **Динамический расчёт цены** на основе типа работы и срочности
- ✅ **Админ-чат** - отправка информации о заказах администратору
- ✅ **Система поддержки** - пользователи могут задавать вопросы
- ✅ **Информация о способах оплаты** - без встроенной оплаты Telegram
- ✅ **Структурированный код** - модульная архитектура

## 📁 Структура проекта

```
bot.py                 # Главный файл запуска бота
config.py              # Конфигурация (токен, цены, реквизиты)
states.py              # FSM состояния
keyboards.py           # Клавиатуры (inline и reply)
handlers.py            # Обработчики команд и главного меню
order_handlers.py      # Обработчики процесса заказа
order_service.py       # Бизнес-логика заказов
requirements.txt       # Зависимости
README.md              # Этот файл
```

## 🔧 Установка

### 1. Клонируйте или скачайте проект

```bash
cd bot677
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте конфигурацию

Откройте файл `config.py` и установите:

```python
# Токен вашего бота (уже заполнен)
BOT_TOKEN = "ваш_токен_сюда"

# ⚠️ ВАЖНО: Установите ID вашего админ-чата!
# Как получить ID админ-чата:
# 1. Создайте закрытую группу в Telegram
# 2. Добавьте бота в группу
# 3. Отправьте сообщение в группу
# 4. Запустите: curl "https://api.telegram.org/botТОКЕН/getUpdates"
# 5. Найдите "chat":{"id":-123456789...}
# 6. Скопируйте этот ID в ADMIN_CHAT_ID

ADMIN_CHAT_ID = -1001234567890  # ← Замените на ваш ID

# Настройте базовые цены (в тенге или вашей валюте)
BASE_PRICES = {
    "presentation": 5000,      # Презентация
    "report": 4000,            # Доклад
    "essay": 3000,             # Эссе
    "other": 5000,             # Другое
}

# Коэффициенты срочности
URGENCY_MULTIPLIERS = {
    "urgent": 1.5,             # Срочно (до 2 часов) - +50%
    "standard": 1.0,           # Стандартно (3-4 часа) - базовая цена
    "no_rush": 0.7,            # Без срочности - -30%
}

# Установите свои реквизиты оплаты
PAYMENT_DETAILS = """
💳 **СПОСОБЫ ОПЛАТЫ:**

**1. Банковская карта (Kaspi Bank)**
Карта: 4400 3096 3287 8149
Имя: Ваше имя

**2. Мобильный кошелёк Kaspi**
+7 (XXX) XXX-XX-XX
"""
```

## 🎮 Запуск

### На Windows

```bash
python bot.py
```

Или используйте `run.bat`:

```bash
run.bat
```

### На Linux/Mac

```bash
python3 bot.py
```

## 📖 Как это работает

### Для пользователя

1. **Нажимает /start** → видит главное меню
2. **Выбирает "Заказать"** → процесс оформления:
   - Выбирает тип работы (презентация, доклад и т.д.)
   - Выбирает срочность (срочно, стандартно, без срочности)
   - Пишет детали заказа
   - Видит итоговую цену
   - Подтверждает заказ
   - Видит реквизиты оплаты
   - После оплаты нажимает "Я оплатил"

3. **Выбирает "Поддержка"** → может задать вопрос
4. **Выбирает "Способы оплаты"** → видит информацию об оплате

### Для администратора

1. В админ-чате получает **уведомление о новом заказе** со всей информацией
2. Выполняет работу вручную
3. Отправляет результат клиенту (напрямую или через бота)

## 📊 Пример уведомления в админ-чате

```
🔔 НОВЫЙ ЗАКАЗ

👤 Клиент: @username
🆔 User ID: `123456789`
📦 Заказ ID: `#1001`

📋 Информация о заказе:
• Тип работы: Презентация
• Срочность: Срочно (до 2 часов)
• Цена: 7500 тг

📝 Детали:
На тему "Климатические изменения"
20 слайдов
Требуется англ. версия

📅 Время заказа: 2026-01-31T15:53:00
✅ Статус: new
```

## 🔐 Безопасность

- **Токен сохранён** в `config.py` (для MVP)
- Для продакшена используйте переменные окружения:

```python
import os
from dotenv import load_dotenv

load_dotenv()
BOT_TOKEN = os.getenv("BOT_TOKEN")
ADMIN_CHAT_ID = int(os.getenv("ADMIN_CHAT_ID"))
```

## 💾 Хранение данных

- **MVP версия** использует память (словарь)
- Заказы теряются при перезагрузке бота
- Для постоянного хранения добавьте БД (SQLite, PostgreSQL и т.д.)

## 🎨 Кастомизация

### Изменить типы работ

В `config.py`:
```python
BASE_PRICES = {
    "presentation": 5000,
    "report": 4000,
    "custom_type": 3000,  # Добавьте новый тип
}
```

В `keyboards.py` обновите кнопки:
```python
[InlineKeyboardButton(text="🎨 Собственный тип", callback_data="work_type_custom_type")],
```

### Изменить коэффициенты срочности

```python
URGENCY_MULTIPLIERS = {
    "very_urgent": 2.0,    # +100%
    "urgent": 1.5,         # +50%
    "standard": 1.0,       # базовая цена
    "no_rush": 0.5,        # -50%
}
```

## 🚨 Решение проблем

### Ошибка: "Ошибка при отправке в админ-чат"

**Решение:**
1. Убедитесь, что `ADMIN_CHAT_ID` установлен правильно
2. Проверьте, что бот добавлен в администраторы группы
3. Используйте команду для получения ID:
   ```bash
   curl "https://api.telegram.org/botВАШ_ТОКЕН/getUpdates"
   ```

### Ошибка: "aiogram not found"

**Решение:**
```bash
pip install -r requirements.txt
```

### Бот не отвечает

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь, что токен верный в `config.py`
3. Запустите заново: `python bot.py`

## 📚 Дополнительные возможности (для будущих версий)

- [ ] Интеграция с БД (SQLite/PostgreSQL)
- [ ] Веб-панель администратора
- [ ] История заказов для пользователя
- [ ] Email-уведомления
- [ ] Автоматическая проверка оплаты
- [ ] Встроенная оплата Telegram (опционально)
- [ ] Многоязычность

## 📄 Лицензия

MIT License - свободно используйте для своих проектов

## 👨‍💻 Разработка

Проект создан как MVP для быстрого запуска. Весь код структурирован и легко расширяется.

### Основные файлы:
- `bot.py` - точка входа и инициализация
- `config.py` - все настройки в одном месте
- `states.py` - управление состояниями
- `keyboards.py` - UI элементы
- `handlers.py` - основные команды
- `order_handlers.py` - логика заказов
- `order_service.py` - бизнес-логика

## 💬 Поддержка

Если у вас есть вопросы, отредактируйте код под свои нужды или добавьте дополнительные функции!

---

**Готово к использованию!** 🎉 Просто установите зависимости, укажите ADMIN_CHAT_ID и запустите бота.
"# telegram-bot-orders" 
