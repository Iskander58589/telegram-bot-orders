# 🚀 РАЗВЕРТЫВАНИЕ БОТА НА HETZNER - ПОЛНОЕ РЕЗЮМЕ

## 📝 Что мы сделали для безопасности

### ✅ Готовые файлы:

1. **`.gitignore`** - защита от утечки токенов
   - `.env` не попадет на GitHub ✓
   - `__pycache__/` не попадет ✓

2. **`config.py`** - обновлен на переменные окружения
   ```python
   BOT_TOKEN = os.getenv("BOT_TOKEN")
   ADMIN_CHAT_ID = int(os.getenv("ADMIN_CHAT_ID"))
   ```

3. **`.env.example`** - шаблон для развертывания
   - Показывает какие переменные нужны
   - Реальный `.env` на сервере создается при развертывании

4. **`setup.sh`** - полностью автоматизированный скрипт развертывания
   - Устанавливает Python
   - Клонирует репо
   - Создает виртуальное окружение
   - Создает systemd сервис

5. **Документация:**
   - `HETZNER_DEPLOYMENT.md` - полный пошаговый гайд (130+ строк)
   - `QUICK_SETUP.md` - быстрый чеклист
   - `HOW_IT_WORKS.md` - как всё взаимодействует

---

## 🎯 ПЛАН ДЕЙСТВИЙ (Что делать СЕЙЧАС)

### ШАГИ 1-3: ЛОКАЛЬНО НА ТВОЕМ ПК (10 минут)

#### Шаг 1: Создаешь GitHub репозиторий

1. Идешь на [github.com](https://github.com)
2. Нажимаешь "New repository"
3. Называешь: `telegram-bot-orders` (или как хочешь)
4. **Обязательно выбираешь "Private"** ← это важно!
5. Нажимаешь "Create repository"

**Зачем Private?** Чтобы никто не видел токен и ADMIN_CHAT_ID (если они случайно попадут)

---

#### Шаг 2: Инициализируешь Git локально

Открываешь PowerShell в папке бота и пишешь:

```powershell
# Перейти в папку бота
cd c:\Users\ASUS\Videos\.gallery\4e2e315c-1878-42fb-8de7-c57a9b83f6dc\bot677

# Инициализируем Git
git init

# Добавляем все файлы (кроме .env - он в .gitignore)
git add .

# Делаем первый коммит
git commit -m "Initial commit: telegram orders bot"

# Добавляем GitHub как удаленный репозиторий
# Замени YOUR_USERNAME на свой юзернейм GitHub
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot-orders.git

# Переименовываем главную ветку (GitHub требует main)
git branch -M main

# Пушим все на GitHub
git push -u origin main
```

**Что произойдет:**
- Весь код загружается на GitHub ✓
- `.env` НЕ загружается (он в `.gitignore`) ✓
- Можешь увидеть свой репо на github.com ✓

---

#### Шаг 3: Добавляешь GitHub Secrets (токены)

Это САМОЕ ВАЖНОЕ для безопасности!

1. Идешь на свой GitHub репо
2. Нажимаешь **Settings** (вкладка в репо)
3. Слева выбираешь **Secrets and variables → Actions**
4. Нажимаешь **New repository secret**

Добавляешь ПЕРВЫЙ секрет:
```
Name: BOT_TOKEN
Value: 8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM
```

Нажимаешь "Add secret" ✓

Добавляешь ВТОРОЙ секрет:
```
Name: ADMIN_CHAT_ID
Value: -1003898978688
```

Нажимаешь "Add secret" ✓

**Результат:**
- Токены НИКОГДА не попадут на GitHub код ✓
- Только ты можешь их увидеть на странице Secrets ✓
- При развертывании сервер получит их через GitHub API ✓

---

### ШАГИ 4-6: СОЗДАНИЕ HETZNER СЕРВЕРА (5 минут)

#### Шаг 4: Регистрируешься на Hetzner

1. Идешь на [hetzner.com](https://www.hetzner.com)
2. Нажимаешь **"Sign Up"**
3. Регистрируешься (почта, пароль, и т.д.)
4. Подтверждаешь почту

**Цена:** От €3.50/месяц за CX11 (хватает для одного бота)

---

#### Шаг 5: Создаешь SSH ключ (для безопасного подключения)

**На Windows (PowerShell):**

```powershell
# Генерируем SSH ключ (если его еще нет)
ssh-keygen -t rsa -b 4096 -f $env:USERPROFILE\.ssh\id_rsa

# Просто жми Enter на все вопросы

# Копируем ПУБЛИЧНЫЙ ключ в буфер обмена
type $env:USERPROFILE\.ssh\id_rsa.pub | clip

# Ключ скопирован! (используем его в Hetzner)
```

**Результат:**
- Публичный ключ скопирован в буфер обмена ✓
- Приватный ключ остался на твоем ПК (его никому не даем) ✓

---

#### Шаг 6: Создаешь сервер в Hetzner консоли

1. Идешь в консоль Hetzner
2. Нажимаешь **"Create Server"** (или "Create Cloud Server")
3. Выбираешь:

**Операционная система:** Ubuntu 22.04 LTS

**Локация:** Frankfurt am Main (или Nuremberg) - ближе к России

**Тип сервера:** CX11 (самый дешевый, для бота подходит)

**SSH Key:** Нажимаешь "Add SSH Key"
   - Вставляешь публичный ключ что скопировал выше
   - Даешь ему имя: "my-bot-key" (или как хочешь)

**Нажимаешь "Create & Buy"**

**Результат:**
- Сервер создается (2-3 минуты) ✓
- Тебе приходит письмо с IP адресом ✓
- Сохраняешь IP в безопасное место ✓

---

### ШАГИ 7-8: РАЗВЕРТЫВАНИЕ КОДА НА СЕРВЕР

#### Шаг 7: Первое подключение к серверу

Открываешь PowerShell и пишешь:

```powershell
# Замени 138.201.123.456 на свой реальный IP из Hetzner
ssh root@138.201.123.456

# При первом подключении спросит - вводишь "yes"
# Готово! Ты теперь на сервере
```

**Результат:**
- Подключился к серверу ✓
- Видишь приглашение `root@…#` ✓

---

#### Шаг 8: Развертывание (ВЫБЕРИ ОДИН ВАРИАНТ)

**ВАРИАНТ A - АВТОМАТИЧЕСКИЙ (1 команда):**

На сервере вводишь:

```bash
curl -O https://raw.githubusercontent.com/YOUR_USERNAME/telegram-bot-orders/main/setup.sh && chmod +x setup.sh && BOT_TOKEN="8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM" ADMIN_CHAT_ID="-1003898978688" bash setup.sh
```

Скрипт автоматически:
- Обновит систему
- Установит Python
- Клонирует репо с GitHub
- Создаст виртуальное окружение
- Установит зависимости
- Создаст systemd сервис
- Запустит бота

**Готово! Бот работает!** ✓

---

**ВАРИАНТ B - ВРУЧНУЮ (если A не сработал):**

```bash
# На сервере вводишь эти команды по одной

# 1. Обновляем систему
apt update && apt upgrade -y

# 2. Устанавливаем Python и Git
apt install -y python3 python3-pip python3-venv git

# 3. Создаем папку проекта
cd /home && mkdir -p telegram-bot && cd telegram-bot

# 4. Клонируем твой репо
git clone https://github.com/YOUR_USERNAME/telegram-bot-orders.git .

# 5. Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 6. Устанавливаем зависимости
pip install -r requirements.txt

# 7. Создаем .env файл с твоими секретами
cat > .env << 'EOF'
BOT_TOKEN=8283115795:AAHRJHjwXivexbPDH-HZg3ydCahBHlzROyM
ADMIN_CHAT_ID=-1003898978688
EOF

# 8. Защищаем .env от других
chmod 600 .env

# 9. Тестируем бота (жми Ctrl+C после)
python3 bot.py

# 10. Создаем systemd сервис
sudo tee /etc/systemd/system/telegram-bot.service > /dev/null << 'EOF'
[Unit]
Description=Telegram Orders Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/home/telegram-bot
Environment="PATH=/home/telegram-bot/venv/bin"
ExecStart=/home/telegram-bot/venv/bin/python3 /home/telegram-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 11. Перезагружаем systemd
systemctl daemon-reload

# 12. Запускаем бота
systemctl start telegram-bot
systemctl enable telegram-bot

# 13. Проверяем статус
systemctl status telegram-bot
```

**Результат:**
- Все установлено ✓
- Бот запущен и работает 24/7 ✓
- Автоматически перезапускается при падении ✓

---

## ✅ ПРОВЕРКА (Бот работает?)

```bash
# На сервере проверяешь статус
systemctl status telegram-bot

# Должно быть: "active (running)" ✓
```

Если видишь "active (running)" - **ПОЗДРАВЛЯЮ! БОТ РАБОТАЕТ!** 🎉

---

## 🧪 ФИНАЛЬНЫЙ ТЕСТ

1. Открываешь Telegram
2. Находишь своего бота
3. Пишешь `/start`
4. Если ответит - **ВСЕ РАБОТАЕТ!** ✅

---

## 📊 ПОСЛЕ РАЗВЕРТЫВАНИЯ

### Команды для управления:

```bash
# Проверить статус
systemctl status telegram-bot

# Посмотреть логи (последние 50 строк)
journalctl -u telegram-bot -n 50

# Логи в реальном времени
journalctl -u telegram-bot -f

# Перезапустить бота
systemctl restart telegram-bot

# Остановить
systemctl stop telegram-bot

# Запустить
systemctl start telegram-bot
```

### После обновления кода на GitHub:

```bash
# На сервере
cd /home/telegram-bot
git pull origin main
systemctl restart telegram-bot

# Готово!
```

---

## 🔐 БЕЗОПАСНОСТЬ (ВАЖНО!)

✅ Что мы сделали правильно:
- Токен в переменных окружения (не в коде)
- GitHub репо приватный
- Secrets хранятся в GitHub Secrets
- .env на сервере имеет права 600 (только владелец читает)
- .env в .gitignore (не попадет на GitHub)

---

## 📞 ЧТО ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Бот не запускается?
```bash
journalctl -u telegram-bot -n 50
# Ищи ошибку в последних 50 строк логов
```

### Проблема с токеном?
```bash
cat /home/telegram-bot/.env
# Проверь есть ли BOT_TOKEN и ADMIN_CHAT_ID
```

### Git не работает?
```bash
cd /home/telegram-bot
git status
git log --oneline
```

### Python не запускается?
```bash
which python3
source venv/bin/activate
python3 --version
```

---

## 🎯 ИТОГО

| Что | Где | Статус |
|-----|-----|--------|
| Код | GitHub (приватный) | ✅ |
| Токены | GitHub Secrets | ✅ |
| .env | На сервере Hetzner | ✅ |
| Бот | Systemd сервис (24/7) | ✅ |
| Пользователи | Telegram | ✅ |

**ГОТОВО К БОЕВОЙ ЭКСПЛУАТАЦИИ!** 🚀

---

## 📚 Документация для справки

- `HETZNER_DEPLOYMENT.md` - подробный гайд
- `QUICK_SETUP.md` - быстрый чеклист
- `HOW_IT_WORKS.md` - как всё взаимодействует

Удачи! 🎉
